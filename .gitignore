# Secrets — NEVER commit
.secrets/
*.key
*.env
.env

# Large precomputed artifacts (hosted on Google Drive; fetched by download_embeddings.py)
artifacts/*.npy
artifacts/*.zip

# Local timing/scratch + dev/backup artifacts (never part of the submission)
sample*.jsonl
out_*.csv
artifacts_live/
artifacts/*_opus.*
artifacts/*_200.*

# Python
__pycache__/
*.pyc
.cache/
